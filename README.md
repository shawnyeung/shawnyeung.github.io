# 遇到的一个坑

当你为了方便归类，在_posts 文件夹添加了子文件夹作为category时，<u>**请务必确保**</u>

1. 文件夹名使用英文字符，不要用中文

2. post文章的头部信息里，categories字段不能出现纯数字



不然当你使用下列命令

```terminal
$ bash tools/run.sh -r
```

在本地预览时会报错，无法实现「即时」预览或支持报错。

